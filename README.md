# ✂️ SubSlash — 구독, 끊을 용기

> 매달 빠져나가는 구독료, 정말 그만한 가치가 있을까요?

**SubSlash**는 구독 서비스의 실제 가치를 추적하고, 불필요한 결제를 즉시 차단(해지)하도록 돕는 **능동형 구독 디톡스 웹 서비스**입니다.

## 🎯 핵심 기능

### 1. 💰 1회 사용 단가(Cost-Per-Use) 체감 엔진

- 결제일 3일 전, 이메일로 단 하나의 질문: **"지난 30일 동안 몇 번 이용하셨나요?"**
- 메일 본문의 `0회 / 1회 / 3회 / 5회 / 10회` 버튼을 한 번 누르면 바로 결과 화면
- 충격 요법 UI: `"이번 달 영화 1편을 ₩17,000에 보셨습니다"`
- 가성비 신호등: 🟢 유지 · 🟡 주의 · 🔴 해지 권고

### 2. 🔪 해지 직통 링크 (Kill-Switch)

- 국내외 주요 29개 구독 서비스의 해지 링크와 단계별 안내
- 링크가 해지 화면으로 바로 가는지(`direct`), 서비스 첫 화면까지만 데려다주는지
  (`entry`)를 프리셋에 기록해 화면에서 구분해 표시합니다. 쿠팡·챗GPT처럼 해지
  화면에 고정 URL이 없는 곳까지 "1초 해지"라고 말하면, 링크를 눌러본 사람은
  나머지 안내도 믿지 않게 됩니다
- 다크 패턴 탈출 30초 가이드

### 3. 👥 나눠 쓰는 구독 분담

- 가족 요금제처럼 여러 명이 나눠 내는 구독은 인원수를 지정하면 **내가 실제로 내는
  몫**으로 지출과 절약액을 집계합니다 (균등 분할이 아니면 부담금 직접 입력)
- 결제일마다 보낼 정산 문구를 클립보드로 복사

### 4. 📊 방어 자산(Saved Pot) 시각화

- 해지 완료 구독의 연간 절약 금액 누적
- "맛있는 치킨 3마리를 살 수 있어요" — 절약액이 실제로 감당하는 보상만 보여주고,
  아직 모자라면 아무것도 약속하지 않습니다

## 🏗️ 기술 스택

| 영역       | 기술                         |
| ---------- | ---------------------------- |
| 프레임워크 | Next.js 15 (App Router)      |
| 스타일링   | Tailwind CSS 4               |
| 상태관리   | Zustand + localStorage       |
| DB         | Turso (libSQL) + Drizzle ORM |
| 알림       | Resend (이메일)              |
| 배포       | Vercel (서버리스)            |
| 모노레포   | Turborepo + pnpm             |
| 테스트     | Vitest + Playwright          |

## 📁 프로젝트 구조

```
subslash/
├── apps/web/          # Next.js 웹 앱 (PWA)
│   ├── app/           # App Router 페이지 & API (/, /dashboard, /subs, /savings, /share)
│   ├── components/    # UI 컴포넌트
│   ├── lib/           # 스토어, DB, 유틸
│   └── hooks/         # React 커스텀 훅
├── packages/shared/   # 공유 타입, 유틸, 상수
└── (harness)          # Turborepo, ESLint, Husky, CI/CD
```

> 금액 합산은 `@subslash/shared`의 헬퍼를 사용합니다. 새 합산 로직을 직접 `reduce`로
> 작성하지 말고 이 둘 중 하나를 재사용하세요.
>
> - `sumMonthlyKRW` / `sumAnnualKRW` — 카드에 청구되는 금액
> - `sumMyMonthlyKRW` / `sumMyAnnualKRW` — 공유 구독을 분담한 뒤 **내가 내는 몫**
>
> 사용자에게 보이는 지출·절약 수치는 후자를 씁니다. 4명이서 나누는 구독을 해지해도
> 실제로 아끼는 돈은 4분의 1이기 때문입니다.
>
> USD 환산 환율은 고정 상수가 아니라 사용자 설정값입니다. 컴포넌트에서는
> `useExchangeRate()`로 받아 넘기세요. 헬퍼의 기본 인자(`DEFAULT_EXCHANGE_RATE`)에
> 기대면 "내 환율을 쓴다"고 적힌 화면 옆에 1,350으로 계산한 값이 나옵니다.

## 🚀 시작하기

### 사전 요구사항

- Node.js 20+
- pnpm 9+

### 설치 및 실행

```bash
# 의존성 설치
pnpm install

# 환경 변수 설정
cp .env.example .env.local

# 개발 서버 시작
pnpm dev
```

### 테스트

```bash
# 단위/통합 테스트
pnpm test

# E2E 테스트 (최초 1회: npx playwright install chromium)
pnpm test:e2e

# 린트 & 타입체크
pnpm turbo lint typecheck
```

> 결제일 계산은 `getNextBillingDateFor` / `getDaysUntilBillingFor`를 쓰세요.
> `billingDay`만 받는 `getNextBillingDate`는 월간 전용이라, 연간 구독에 쓰면
> 1년에 한 번인 결제를 매달 있는 것으로 계산합니다. 두 함수는 결제 월이 없는
> 연간 구독에 `null`을 반환하며, 이때 화면은 숫자가 아니라 '미설정'을 보여줘야
> 합니다.

## 🌟 Zero-Friction 온보딩

회원가입 없이 바로 시작합니다:

1. 첫 방문 시 구독 3개를 즉시 등록
2. 브라우저 localStorage에 데이터 저장
3. 알림이 필요할 때만 이메일 입력

## 🔔 결제 알림 아키텍처 (미러 모델)

기본 경험은 100% 로컬·익명입니다. 서버는 **알림을 켠 사용자에 한해서만**,
그것도 알림에 필요한 최소 정보만 보관합니다.

```
localStorage (원본)  ──PUT /api/notify/sync──▶  서버 미러
    │                                              │
    │ 체크인·절약자산·해지구독·연동계정              │ 이메일 · 이름 · 금액 · 결제일
    │ (전송되지 않음)                               ▼
    └──────◀── /check-in?sub=&count= ────  일일 크론 → Resend 이메일
```

- **단방향 동기화**: 서버는 절대 클라이언트로 되쓰지 않습니다. 동기화는 전체 교체
  방식이라 병합·충돌 해결이 없고, 마지막으로 동기화한 기기가 서버 상태를 정의합니다.
- **옵트인 + 이메일 확인**: 확인 링크를 누르기 전에는 어떤 알림도 발송되지 않습니다.
- **원탭 체크인**: 메일의 버튼은 서버를 거치지 않고 해당 기기의 localStorage에
  기록합니다. 따라서 토큰도, 왕복도 없습니다.
- **수신 거부 = 완전 삭제**: 서버가 갖고 있던 모든 행이 함께 지워집니다.

### 📅 캘린더 구독 (ICS 피드)

알림을 켜면 `/api/calendar/<token>.ics` 주소가 발급됩니다. 캘린더 앱에 URL로
구독해두면 결제일이 반복 일정으로 뜨고, 설정한 일수 전에 캘린더 자체 알림이
울립니다. 앱에서 구독을 고치면 다음 폴링 때 캘린더에도 반영됩니다.

- **읽기 전용 토큰**: 캘린더 URL은 앱에 붙여넣고 기기 간에 동기화되며 때로는
  공유되므로, 미러를 덮어쓸 수 있는 sync 토큰과 분리했습니다. 서버는 해시만
  보관하고, 새 주소를 만들면 이전 주소로 구독한 캘린더가 전부 끊깁니다.
- **확인 메일 불필요**: 메일을 보내지 않으므로 이메일 확인 전에도 동작합니다.
- **연간 결제 구독**: 결제 월(`billingMonth`)이 등록돼 있으면 `FREQ=YEARLY`로
  내보냅니다. 등록 전이라면 날짜를 특정할 수 없으므로 제외되고, 앱은 D-day 자리에
  '결제 월 미설정'을 표시해 채워달라고 요청합니다.

### 로컬에서 알림 흐름 테스트하기

```bash
cp .env.example apps/web/.env.local   # CRON_SECRET만 채우면 충분합니다
cd apps/web && pnpm db:push           # TURSO_* 미설정 시 로컬 SQLite 사용
pnpm dev

# RESEND_API_KEY가 없으면 메일을 보내지 않고 서버 로그에 출력합니다
curl -H "Authorization: Bearer <CRON_SECRET>" http://localhost:3000/api/cron/notify
```

## 🔐 로그인 / 회원가입 (선택)

계정은 **선택 기능**입니다. 로그인하지 않아도 앱의 모든 기능이 그대로 동작하며,
구독 데이터는 여전히 브라우저 안에 있습니다. 계정은 기기를 옮길 때를 위한 것입니다.

가입 시 받는 값은 아이디, 이메일, 비밀번호(+확인), 나이, 성별입니다.

- **비밀번호는 되돌릴 수 없게 저장합니다.** 복호화 가능한 암호문이 아니라 사용자마다
  다른 솔트를 붙인 scrypt 해시(`scrypt$N$r$p$솔트$해시`)로만 남습니다. 복호화할 수
  있게 두면 DB와 키가 함께 새는 순간 전부 평문이 되고, 그 전에도 서버가 사용자의
  비밀번호를 알 수 있게 됩니다. 저희는 알 필요가 없고, 알아서도 안 됩니다.
  해시 비용을 올리면 다음 로그인 때 자동으로 다시 해시합니다.
- **입력값은 쿼리문에 이어 붙지 않습니다.** 모든 조회·삽입은 Drizzle 쿼리 빌더로만
  나가고, 값은 SQL 문자열이 아니라 파라미터로 분리돼 전달됩니다. 따옴표든
  `DROP TABLE`이든 한 칸의 데이터로 저장될 뿐 명령이 되지 않습니다
  (`__tests__/integration/auth-flow.test.ts`가 실제 페이로드로 확인합니다).
  `lib/auth-server.ts`에 `sql.raw`나 문자열 조합 쿼리를 추가하면 이 보장이 깨집니다.
- **자격증명은 본문으로만 보냅니다.** 비밀번호를 쿼리스트링에 실으면 브라우저 기록과
  서버 접근 로그에 그대로 남기 때문입니다.
- **세션**: 임의 토큰을 httpOnly·SameSite=Lax 쿠키로 내리고, 서버에는 그 SHA-256만
  저장합니다(sync·캘린더 토큰과 같은 규칙). 로그아웃하면 서버 행도 함께 지웁니다.
- **로그인 실패 메시지는 하나뿐입니다.** "없는 아이디"와 "틀린 비밀번호"를 나눠
  알려주면 어떤 아이디가 가입돼 있는지 훑어낼 수 있어, 응답 시간까지 맞춥니다.
- 만 14세 미만은 법정대리인 동의가 필요해 지금은 받지 않습니다.

> [!IMPORTANT]
> 계정 테이블(`accounts`, `sessions`)은 마이그레이션 `0003`에서 생깁니다. CI는
> 마이그레이션을 적용하지 않으므로, **배포 전에 대상 DB에 직접 적용해야 합니다.**
> 적용하지 않으면 가입 요청이 `no such table: accounts`로 500을 냅니다.
>
> ```bash
> cd apps/web
> TURSO_DATABASE_URL=... TURSO_AUTH_TOKEN=... pnpm db:push
> ```

## 📱 모바일 / PWA

- 웹 매니페스트 + 서비스 워커(`public/sw.js`)로 설치형 PWA로 동작합니다. 홈 화면에
  추가하면 전체 화면으로 실행되고, 네트워크가 없을 때는 `/offline` 안내 화면이 뜹니다.
- Android: 설치 후 공유 시트에서 SubSlash를 고르면 결제 문자·영수증이 `/share`로
  넘어와 그대로 파싱됩니다 (`manifest.json`의 `share_target`).
  `/share?text=<결제문자>`로 직접 열어도 동일하게 동작합니다.
- 서비스 워커는 오프라인 안내 화면 하나만 캐시합니다. 구독 데이터는 localStorage에
  있고 페이지도 작아서, 앱 셸을 캐시해봐야 배포 후 옛 빌드가 남을 위험만 커집니다.

## 📄 라이선스

MIT
