# ==========================================
# SubSlash 환경 변수 설정 가이드 (Vercel 배포용)
# ==========================================

# 1. 앱 기본 도메인 — 메일 속 링크(가입 확인·비밀번호 재설정·알림)가 이 주소로 갑니다.
#    운영은 고정 도메인을 씁니다. 미리보기 배포 주소를 넣으면 메일 링크가 그 배포로 갑니다.
NEXT_PUBLIC_APP_URL="https://www.subslash.me"

# 2. 데이터베이스 (Turso Serverless SQLite - 결제일 이메일 알림 기능용, 선택사항)
# * 미설정 시에도 브라우저 LocalStorage 기반 구독 관리/체크인/대시보드는 100% 정상 작동합니다.
# TURSO_DATABASE_URL="libsql://your-database-name.turso.io"
# TURSO_AUTH_TOKEN="your-turso-auth-token"

# 3. 이메일 발송 (Resend - 선택사항): 결제 알림 메일과 가입 확인 메일
# * 발신 주소는 Resend에서 인증(SPF·DKIM)한 도메인이어야 합니다. 운영은 subslash.me를
#   인증해 두었고, RESEND_FROM_EMAIL을 비우면 noreply@subslash.me로 보냅니다.
# * onboarding@resend.dev는 Resend 계정 주인의 주소로만 보낼 수 있습니다(다른 사용자의 확인
#   메일은 거절됨). 도메인 인증 전 혼자 시험할 때만 쓰세요.
# * 미설정 시 가입은 그대로 되지만 확인 메일은 나가지 않고, 화면에 보내지 못했다고 표시됩니다.
# RESEND_API_KEY="re_123456789"
# RESEND_FROM_EMAIL="SubSlash <noreply@subslash.me>"

# 4. Vercel Cron 및 보안 키 (선택사항)
# * EMAIL_LINK_SECRET(없으면 CRON_SECRET)은 메일 속 링크(알림 확인·수신 거부·가입 확인)에
#   서명합니다. 둘 다 없으면 확인 메일을 보내지 않습니다.
# CRON_SECRET="your-random-cron-secret-key"
# EMAIL_LINK_SECRET="your-random-email-link-secret"

# 5. Gmail 원클릭 연결 · 구글 캘린더에 결제일 등록 (선택사항)
# * 운영자가 배포한 SubSlash Apps Script 웹 앱의 주소(https://script.google.com/macros/s/…/exec).
#   `pnpm --filter @subslash/web gmail:web-app -- --origins <이 배포 주소>`로 파일을 만들어 배포합니다.
# * 같은 웹 앱이 '구글 캘린더에 결제일 등록'도 처리합니다.
# * 미설정 시 'Gmail 연결하기'와 '구글 캘린더에 등록하기'는 보이지 않고, 스크립트를 직접 설치하는
#   방식과 알림 설정의 캘린더 구독만 됩니다.
# * 연결 코드 서명에 EMAIL_LINK_SECRET(없으면 CRON_SECRET)을 씁니다.
# GMAIL_CONNECT_WEB_APP_URL="https://script.google.com/macros/s/your-deployment-id/exec"

# 구글·카카오·네이버로 로그인(lib/oauth). 키가 있는 제공자만 버튼이 보이고, lib/privacy.ts의
# SOCIAL_LOGIN_STARTS_ON을 정해야 열린다(테스트 서버는 NEXT_PUBLIC_SOCIAL_LOGIN_TEST_OPEN="true").
# 각 제공자 앱 설정에 돌아올 주소를 등록한다: https://<웹 주소>/api/auth/oauth/<google|kakao|naver>/callback
# 동의 항목은 이메일만 받는다(카카오: 카카오계정(이메일), 네이버: 이메일 주소).
# GOOGLE_OAUTH_CLIENT_ID=""
# GOOGLE_OAUTH_CLIENT_SECRET=""
# KAKAO_OAUTH_CLIENT_ID=""        # REST API 키
# KAKAO_OAUTH_CLIENT_SECRET=""    # 보안 › Client Secret을 켰을 때만
# NAVER_OAUTH_CLIENT_ID=""
# NAVER_OAUTH_CLIENT_SECRET=""
